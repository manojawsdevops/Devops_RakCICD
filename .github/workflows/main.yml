name: ci
on :
  push: 
    branches: [main]
  pull_request_review:
    branches: [main]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: action/checkout@v2
      - name: Run echo
        run: echo hello world
      - name: Run shh
        run: shell.sh
 
    
